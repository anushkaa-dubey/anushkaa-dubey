name: Update Credly Badges

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-badges:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Update badges
        uses: pemtajo/badge-readme@main
        with:
          CREDLY_USER: "anushka-dubey.fed1f29c"
